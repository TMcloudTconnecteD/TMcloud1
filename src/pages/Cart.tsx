// src/pages/Cart.tsx
import React from "react";
import CartComponent from "../components/CartComponent/CartComponent"; // Import the CartComponent

const Cart: React.FC = () => {
  return (
    <div>
      <CartComponent /> {/* Render the CartComponent */}
    </div>
  );
};

export default Cart;
